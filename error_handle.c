#include "push_swap.h"

void error_handle(int index)
{
    if (index == 1)
        printf("Error - incorrect arguments");
    exit(1);
    else if (index == 2)
        printf("Error - incorrect operation")
}
